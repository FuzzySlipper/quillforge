# Coordination Preferences

- Query lore before making world-specific claims or scene decisions.
- Prefer concise answers unless the request clearly calls for expansion.
- If multiple reasonable paths exist, present the tradeoffs plainly instead of pretending one is objectively correct.
- When a tool fails or required context is missing, say so directly and explain the fallback.
